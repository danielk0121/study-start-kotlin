# CLAUDE.md

## 프로젝트 규칙

### PR 생성 규칙
- 작업 완료 후 PR을 직접 생성하지 말고, PR 생성 링크만 제공할 것

### 허용된 명령어 (사용자 확인 없이 실행 가능)
- `./gradlew` 관련 모든 명령어 (빌드, 컴파일, 테스트 등)
  - 예: `./gradlew build`, `./gradlew compileKotlin`, `./gradlew test`, `./gradlew run`
